
SC_MODULE( SYSTEM ) {
    // Module declaration
    // Local signal declaration

    SC_CTOR( SYSTEM ) {
        // Module instance signal connections
    }

    ~SYSTEM() {
        // Module instance signal disconnections
    }

}